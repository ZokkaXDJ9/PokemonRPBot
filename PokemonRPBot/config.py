# config.py

# List of commands to load (use the full path to each command module)
COMMANDS = [
    "commands.ability",
    "commands.r",
    "commands.roll",
    "commands.rule",
    "commands.status",
    "commands.move",
    "commands.weather",
    "commands.suggest",
    "commands.stats",
]
